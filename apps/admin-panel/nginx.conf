server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # HashRouter: arquivos estaticos normais, sem necessidade de fallback de rota.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
